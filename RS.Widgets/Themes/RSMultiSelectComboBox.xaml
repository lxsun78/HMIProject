<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:RS.Widgets.Converters"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:model="clr-namespace:RS.Widgets.Models"
    xmlns:rs="clr-namespace:RS.Widgets.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.Border.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.Button.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.ComboBoxItem.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.DataGrid.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.Effects.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.Icons.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.Path.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.RepeatButton.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.Templates.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.TextBox.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.ToggleButton.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <converters:ClipConverter x:Key="ClipConverter" />
    <MenuScrollingVisibilityConverter x:Key="MenuScrollingVisibilityConverter" />

    <Style BasedOn="{StaticResource RSApp.Styles.DataGridStyle}" TargetType="{x:Type rs:RSMultiSelectComboBox}">
        <Setter Property="Foreground" Value="#FF333333" />
        <Setter Property="BorderBrush" Value="#eee" />
        <Setter Property="Background" Value="#fff" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="MinRowHeight" Value="32" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="VerticalGridLinesBrush" Value="#e5e6eb" />
        <Setter Property="HorizontalGridLinesBrush" Value="#e5e6eb" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="AutoGenerateColumns" Value="False" />
        <Setter Property="RowDetailsVisibilityMode" Value="VisibleWhenSelected" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="SelectionMode" Value="Extended" />
        <Setter Property="CanUserAddRows" Value="False" />
        <Setter Property="CanUserDeleteRows" Value="False" />
        <Setter Property="CanUserReorderColumns" Value="False" />
        <Setter Property="CanUserResizeRows" Value="False" />
        <Setter Property="CanUserSortColumns" Value="False" />
        <Setter Property="EnableRowVirtualization" Value="True" />
        <Setter Property="EnableColumnVirtualization" Value="True" />
        <Setter Property="rs:ControlsHelper.IconHeight" Value="40" />
        <Setter Property="rs:ControlsHelper.IconWidth" Value="50" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="rs:TextBoxHelper.IsShowClearButton" Value="True" />
        <Setter Property="rs:TextBoxHelper.Watermark" Value="请选择内容" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="#eeeeee" />
        <Setter Property="rs:ControlsHelper.CornerRadius" Value="3" />
        <Setter Property="Margin" Value="3,3,3,3" />
        <Setter Property="Padding" Value="2,3,2,3" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="HeadersVisibility" Value="None" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type rs:RSMultiSelectComboBox}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding rs:ControlsHelper.CornerRadius}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="20" />
                            </Grid.ColumnDefinitions>

                            <ContentControl
                                x:Name="PART_Watermark"
                                Grid.Column="0"
                                Margin="2,0,0,0"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding rs:TextBoxHelper.Watermark}"
                                FontSize="{TemplateBinding FontSize}"
                                Foreground="{TemplateBinding rs:TextBoxHelper.WatermarkForground}"
                                Visibility="Collapsed" />

                            <Grid x:Name="PART_ContentGrid">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="auto" />
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="0" Background="Transparent">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="MouseLeftButtonUp">
                                            <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=ToggleDropDownCommand}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                    <ScrollViewer
                                        Background="Transparent"
                                        CanContentScroll="True"
                                        Focusable="False"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Disabled">
                                        <ItemsControl x:Name="PART_TagContent" ItemsSource="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=TagModelList}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type model:TagModel}">
                                                    <Border
                                                        x:Name="PART_TagItemBorder"
                                                        Margin="2,0,2,0"
                                                        Padding="5,0,5,0"
                                                        Background="#E7EAED"
                                                        CornerRadius="5"
                                                        Focusable="False">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition />
                                                                <ColumnDefinition Width="auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <Grid Background="Transparent">
                                                                <i:Interaction.Triggers>
                                                                    <i:EventTrigger EventName="MouseLeftButtonUp">
                                                                        <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type rs:RSMultiSelectComboBox}}, Path=TagSelectCommand}" CommandParameter="{Binding}" />
                                                                    </i:EventTrigger>
                                                                </i:Interaction.Triggers>
                                                                <ContentControl x:Name="PART_TagItemContent" Content="{Binding TagContent}" />
                                                            </Grid>

                                                            <Border
                                                                x:Name="PART_TagItemCloseButtonHost"
                                                                Grid.Column="1"
                                                                Width="8"
                                                                Height="8"
                                                                Margin="2,0,2,0"
                                                                Background="Transparent">
                                                                <i:Interaction.Triggers>
                                                                    <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                                                                        <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type rs:RSMultiSelectComboBox}}, Path=TagCloseCommand}" CommandParameter="{Binding}" />
                                                                    </i:EventTrigger>
                                                                </i:Interaction.Triggers>
                                                                <Path
                                                                    x:Name="PART_TagItemCloseIcon"
                                                                    Data="{StaticResource RSApp.Icons.Close}"
                                                                    Fill="#4D182B40"
                                                                    Stretch="Fill" />
                                                            </Border>

                                                        </Grid>
                                                    </Border>
                                                    <DataTemplate.Triggers>
                                                        <Trigger SourceName="PART_TagItemBorder" Property="IsMouseOver" Value="True">
                                                            <Setter Property="Cursor" Value="Hand" />
                                                            <Setter Property="Opacity" Value="0.8" />
                                                        </Trigger>

                                                        <Trigger SourceName="PART_TagItemCloseButtonHost" Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="PART_TagItemCloseIcon" Property="Fill" Value="#CC182B40" />
                                                        </Trigger>

                                                        <DataTrigger Binding="{Binding IsSelect}" Value="True">
                                                            <Setter TargetName="PART_TagItemBorder" Property="Background" Value="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=(rs:ControlsHelper.SelectedColor)}" />
                                                            <Setter TargetName="PART_TagItemContent" Property="Foreground" Value="#FFFFFF" />
                                                            <Setter TargetName="PART_TagItemCloseIcon" Property="Fill" Value="#FFFFFF" />
                                                        </DataTrigger>
                                                    </DataTemplate.Triggers>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Grid>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button
                                        x:Name="PART_ClearAllButton"
                                        Grid.Column="1"
                                        Margin="0"
                                        Padding="5,3,5,3"
                                        HorizontalAlignment="Right"
                                        rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.Close}"
                                        rs:ControlsHelper.IconHeight="12"
                                        rs:ControlsHelper.IconWidth="12"
                                        Command="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=ClearAllTagsCommand}"
                                        Focusable="False"
                                        Foreground="#949494"
                                        Style="{StaticResource RSApp.Styles.IconButtonStyle}"
                                        ToolTip="清除所有标签"
                                        Visibility="Collapsed" />

                                    <ToggleButton
                                        x:Name="PART_MoreButton"
                                        Padding="0"
                                        rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.Ellipsis}"
                                        Focusable="False"
                                        Style="{StaticResource RSApp.Styles.IconButtonStyle}" />
                                </StackPanel>
                            </Grid>

                            <ToggleButton
                                x:Name="PART_ToggleButton"
                                Grid.Column="1"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Focusable="False"
                                IsChecked="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsDropDownOpen, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Style="{StaticResource RSApp.Styles.ComboBoxToggleButtonStyle}">
                                <Path
                                    x:Name="Arrow"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="M 0 0 L 4 4 L 8 0 Z"
                                    Fill="#FF444444" />
                            </ToggleButton>


                            <rs:RSPopup
                                x:Name="PART_Popup"
                                AllowsTransparency="True"
                                Focusable="False"
                                IsOpen="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsDropDownOpen, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                Placement="Bottom"
                                PlacementTarget="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                PopupAnimation="None"
                                RelativeElement="{Binding ElementName=PART_ToggleButton}"
                                StaysOpen="True">
                                <Border
                                    Width="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Margin="15,0,15,15"
                                    Background="#FFF"
                                    CornerRadius="{TemplateBinding rs:ControlsHelper.CornerRadius}"
                                    Effect="{StaticResource RSApp.Styles.DefaultEffect}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <TextBox
                                            x:Name="PART_SearchContent"
                                            rs:TextBoxHelper.Watermark="请输入查询内容"
                                            Text="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=SearchContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=ShowSearchBox, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <ScrollViewer
                                            x:Name="DG_ScrollViewer"
                                            Grid.Row="1"
                                            Focusable="false">
                                            <ScrollViewer.Template>
                                                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="*" />
                                                            <RowDefinition Height="Auto" />
                                                        </Grid.RowDefinitions>
                                                        <Button
                                                            Width="{Binding CellsPanelHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                            Command="{x:Static DataGrid.SelectAllCommand}"
                                                            Focusable="false"
                                                            Style="{DynamicResource {ComponentResourceKey ResourceId=DataGridSelectAllButtonStyle,
                                                                                                          TypeInTargetAssembly={x:Type DataGrid}}}"
                                                            Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.All}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />



                                                        <DataGridColumnHeadersPresenter
                                                            x:Name="PART_ColumnHeadersPresenter"
                                                            Grid.Column="1"
                                                            Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Column}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />


                                                        <Grid Grid.Row="1" Grid.ColumnSpan="2">
                                                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanContentScroll="{TemplateBinding CanContentScroll}" />

                                                            <ContentControl
                                                                x:Name="PART_EmptyData"
                                                                rs:ControlsHelper.EmptyDataDes="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=(rs:ControlsHelper.EmptyDataDes)}"
                                                                rs:ControlsHelper.IconData="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=(rs:ControlsHelper.IconData)}"
                                                                rs:ControlsHelper.IconHeight="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=(rs:ControlsHelper.IconHeight)}"
                                                                rs:ControlsHelper.IconWidth="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}, Path=(rs:ControlsHelper.IconWidth)}"
                                                                Template="{StaticResource RSApp.Templates.EmptyData}"
                                                                Visibility="Collapsed" />
                                                        </Grid>


                                                        <ScrollBar
                                                            x:Name="PART_VerticalScrollBar"
                                                            Grid.Row="1"
                                                            Grid.Column="2"
                                                            Maximum="{TemplateBinding ScrollableHeight}"
                                                            Orientation="Vertical"
                                                            ViewportSize="{TemplateBinding ViewportHeight}"
                                                            Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                                            Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />

                                                        <Grid Grid.Row="2" Grid.Column="1">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="{Binding NonFrozenColumnsViewportHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>

                                                            <ScrollBar
                                                                x:Name="PART_HorizontalScrollBar"
                                                                Grid.Column="1"
                                                                Maximum="{TemplateBinding ScrollableWidth}"
                                                                Orientation="Horizontal"
                                                                ViewportSize="{TemplateBinding ViewportWidth}"
                                                                Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                                                Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
                                                        </Grid>
                                                    </Grid>

                                                    <ControlTemplate.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type DataGrid}}, Path=HasItems}" Value="False">
                                                            <Setter TargetName="PART_EmptyData" Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </ScrollViewer.Template>
                                            <ItemsPresenter />
                                        </ScrollViewer>
                                    </Grid>
                                </Border>
                            </rs:RSPopup>

                            <rs:RSPopup
                                x:Name="PART_MorePopup"
                                AllowsTransparency="True"
                                Focusable="False"
                                HorizontalOffset="-3"
                                IsOpen="{Binding ElementName=PART_MoreButton, Path=IsChecked, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                Placement="Bottom"
                                PlacementTarget="{Binding ElementName=PART_MoreButton}"
                                PopupAnimation="None"
                                RelativeElement="{Binding ElementName=PART_MoreButton}"
                                StaysOpen="True"
                                VerticalOffset="8">
                                <Border
                                    MinWidth="250"
                                    MaxWidth="400"
                                    MaxHeight="400"
                                    Margin="15,0,15,15"
                                    Background="#FFF"
                                    CornerRadius="5"
                                    Effect="{StaticResource RSApp.Styles.DefaultEffect}">
                                    <Grid>
                                        <ScrollViewer
                                            MaxHeight="350"
                                            CanContentScroll="True"
                                            VerticalScrollBarVisibility="Auto">
                                            <ScrollViewer.Style>
                                                <Style BasedOn="{x:Null}" TargetType="{x:Type ScrollViewer}">
                                                    <Setter Property="HorizontalScrollBarVisibility" Value="Visible" />
                                                    <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*" />
                                                                    </Grid.ColumnDefinitions>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="*" />
                                                                        <RowDefinition Height="Auto" />
                                                                    </Grid.RowDefinitions>
                                                                    <Border Grid.Row="1" Grid.Column="0">
                                                                        <ScrollContentPresenter Margin="{TemplateBinding Padding}" CanContentScroll="{TemplateBinding CanContentScroll}" />
                                                                    </Border>
                                                                    <RepeatButton
                                                                        x:Name="PART_BtnUpArrow"
                                                                        Grid.Row="0"
                                                                        Grid.Column="0"
                                                                        Command="{x:Static ScrollBar.LineUpCommand}"
                                                                        CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                                                        Focusable="false"
                                                                        Style="{StaticResource MenuScrollButton}">
                                                                        <RepeatButton.Visibility>
                                                                            <MultiBinding
                                                                                Converter="{StaticResource MenuScrollingVisibilityConverter}"
                                                                                ConverterParameter="0"
                                                                                FallbackValue="Visibility.Collapsed">
                                                                                <Binding Path="ComputedVerticalScrollBarVisibility" RelativeSource="{RelativeSource TemplatedParent}" />
                                                                                <Binding Path="VerticalOffset" RelativeSource="{RelativeSource TemplatedParent}" />
                                                                                <Binding Path="ExtentHeight" RelativeSource="{RelativeSource TemplatedParent}" />
                                                                                <Binding Path="ViewportHeight" RelativeSource="{RelativeSource TemplatedParent}" />
                                                                            </MultiBinding>
                                                                        </RepeatButton.Visibility>
                                                                        <Path
                                                                            x:Name="PART_IconUpArrow"
                                                                            Data="{StaticResource RSApp.Icons.MenuItemUpArrow}"
                                                                            Fill="{TemplateBinding Foreground}" />
                                                                    </RepeatButton>

                                                                    <RepeatButton
                                                                        x:Name="PART_BtnDownArrow"
                                                                        Grid.Row="2"
                                                                        Grid.Column="0"
                                                                        Command="{x:Static ScrollBar.LineDownCommand}"
                                                                        CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                                                        Focusable="false"
                                                                        Style="{StaticResource MenuScrollButton}">
                                                                        <RepeatButton.Visibility>
                                                                            <MultiBinding
                                                                                Converter="{StaticResource MenuScrollingVisibilityConverter}"
                                                                                ConverterParameter="100"
                                                                                FallbackValue="Visibility.Collapsed">
                                                                                <Binding Path="ComputedVerticalScrollBarVisibility" RelativeSource="{RelativeSource TemplatedParent}" />
                                                                                <Binding Path="VerticalOffset" RelativeSource="{RelativeSource TemplatedParent}" />
                                                                                <Binding Path="ExtentHeight" RelativeSource="{RelativeSource TemplatedParent}" />
                                                                                <Binding Path="ViewportHeight" RelativeSource="{RelativeSource TemplatedParent}" />
                                                                            </MultiBinding>
                                                                        </RepeatButton.Visibility>
                                                                        <Path
                                                                            x:Name="PART_IconDownArrow"
                                                                            Data="{StaticResource RSApp.Icons.MenuItemDownArrow}"
                                                                            Fill="{TemplateBinding Foreground}" />
                                                                    </RepeatButton>
                                                                </Grid>

                                                                <ControlTemplate.Triggers>
                                                                    <Trigger SourceName="PART_BtnDownArrow" Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="PART_IconDownArrow" Property="Opacity" Value="0.7" />
                                                                    </Trigger>
                                                                    <Trigger SourceName="PART_BtnUpArrow" Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="PART_IconUpArrow" Property="Opacity" Value="0.7" />
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ScrollViewer.Style>

                                            <ItemsControl Margin="5" ItemsSource="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=TagModelList}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel IsItemsHost="True" Orientation="Vertical" />
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate DataType="{x:Type model:TagModel}">
                                                        <Border
                                                            x:Name="PART_TagItemBorder"
                                                            Height="32"
                                                            Background="Transparent"
                                                            CornerRadius="3"
                                                            Focusable="False">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="2" />
                                                                    <ColumnDefinition />
                                                                    <ColumnDefinition Width="auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <Rectangle
                                                                    x:Name="PART_TagItemSelectionIndicator"
                                                                    Height="15"
                                                                    VerticalAlignment="Center"
                                                                    Fill="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=(rs:ControlsHelper.SelectedColor)}"
                                                                    Visibility="{Binding IsSelect, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                                <Grid Grid.Column="1" Background="Transparent">
                                                                    <i:Interaction.Triggers>
                                                                        <i:EventTrigger EventName="MouseLeftButtonUp">
                                                                            <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type rs:RSMultiSelectComboBox}}, Path=TagSelectCommand}" CommandParameter="{Binding}" />
                                                                        </i:EventTrigger>
                                                                    </i:Interaction.Triggers>
                                                                    <ContentControl
                                                                        VerticalAlignment="Center"
                                                                        Content="{Binding TagContent}"
                                                                        FontSize="12" />
                                                                </Grid>

                                                                <Border
                                                                    x:Name="PART_TagItemCloseButtonHost"
                                                                    Grid.Column="2"
                                                                    Width="12"
                                                                    Height="12"
                                                                    Margin="8,0,5,0"
                                                                    VerticalAlignment="Center"
                                                                    Background="Transparent"
                                                                    Cursor="Hand">
                                                                    <i:Interaction.Triggers>
                                                                        <i:EventTrigger EventName="MouseLeftButtonUp">
                                                                            <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type rs:RSMultiSelectComboBox}}, Path=TagCloseCommand}" CommandParameter="{Binding}" />
                                                                        </i:EventTrigger>
                                                                    </i:Interaction.Triggers>
                                                                    <Path
                                                                        x:Name="PART_TagItemCloseIcon"
                                                                        Data="{StaticResource RSApp.Icons.Close}"
                                                                        Fill="#999999"
                                                                        Stretch="Fill" />
                                                                </Border>
                                                            </Grid>
                                                        </Border>
                                                        <DataTemplate.Triggers>
                                                            <Trigger SourceName="PART_TagItemBorder" Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="PART_TagItemBorder" Property="Background" Value="#F9F9F9" />
                                                                <Setter TargetName="PART_TagItemCloseIcon" Property="Fill" Value="#666666" />
                                                                <Setter TargetName="PART_TagItemCloseIcon" Property="Visibility" Value="Visible" />
                                                            </Trigger>
                                                            <Trigger SourceName="PART_TagItemBorder" Property="IsMouseOver" Value="False">
                                                                <Setter TargetName="PART_TagItemCloseIcon" Property="Visibility" Value="Collapsed" />
                                                            </Trigger>
                                                            <Trigger SourceName="PART_TagItemCloseButtonHost" Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="PART_TagItemCloseIcon" Property="Fill" Value="#333333" />
                                                            </Trigger>
                                                            <DataTrigger Binding="{Binding IsSelect}" Value="True">
                                                                <Setter TargetName="PART_TagItemBorder" Property="Background" Value="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=(rs:ControlsHelper.SelectedBackground)}" />
                                                            </DataTrigger>
                                                        </DataTemplate.Triggers>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Grid>
                                </Border>
                            </rs:RSPopup>
                        </Grid>
                    </Border>



                    <ControlTemplate.Triggers>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="HasContent" Value="True" />
                                <Condition Property="rs:TextBoxHelper.IsShowClearButton" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_ClearAllButton" Property="Visibility" Value="Visible" />
                        </MultiTrigger>

                        <Trigger Property="HasContent" Value="False">
                            <Setter TargetName="PART_MoreButton" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="PART_ClearAllButton" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <Trigger Property="Validation.HasError" Value="True">
                            <Setter Property="BorderBrush" Value="Transparent" />
                        </Trigger>

                        <Trigger Property="HasContent" Value="True">
                            <Setter TargetName="PART_Watermark" Property="Visibility" Value="Collapsed" />
                        </Trigger>

                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=Self}, Path=(rs:TextBoxHelper.IsShowClearButton)}" Value="False">
                            <Setter TargetName="PART_ClearAllButton" Property="Visibility" Value="Collapsed" />
                        </DataTrigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>